const peserta=['Andi','Budi','Citra'];const c=document.getElementById('votingList');peserta.forEach(p=>{const div=document.createElement('div');div.className='p-4 bg-slate-800 rounded flex justify-between';const btn=document.createElement('button');btn.textContent='Vote';btn.className='px-3 py-1 bg-indigo-600 rounded';btn.onclick=()=>{localStorage.setItem('vote',p);alert('Kamu memilih '+p)};div.innerHTML=`<span>${p}</span>`;div.appendChild(btn);c.appendChild(div);});